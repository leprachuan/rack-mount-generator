[project]
name = "rack-mount-generator"
version = "0.1.0"
description = "Rack mount generator for organizing hardware"
requires-python = ">=3.10"
dependencies = [
    "Flask==3.0.0",
    "Flask-CORS==4.0.0",
    "numpy>=1.26.0",
    "Werkzeug==3.0.1",
]
