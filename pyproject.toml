[project]
name = "rack-mount-generator"
version = "2.0.0"
description = "19\" Rack Mount Generator - Client-side STL export via Three.js"
requires-python = ">=3.11"
dependencies = [
    "Flask==3.0.0",
    "Werkzeug==3.0.1",
]
